** Již probráno **
-- Použití vlastního API klíče
-- Použití dotenv-java knihovny pro .env proměnné
-- One-to-One vztah v aplikaci pravděpodobně funguje, co nefunguje je spíše Reverse engineer funkce v MySQL Workbench
-- Přidal jsem E-R Model (nově i s tabulkou users)
-- Opravily se errory při generování testů (conversationHistory)
-- Úspěšně se vkládají a linkují všechna data z vytvořeného testu
-- Přidávání uživatelů
-- Úprava UI
-- Zobrazení informace o nahraném souboru
-- Loader
-- Automatické stáhnutí souboru po vygenerování
-- Generování docx soubor
-- Checkbox "Četl(a) jsem Manuál aplikace"
-- Problém s kódem uvnitř testu
-- Text wrap u TextArea
-- Problém po přesměrování na stránku
-- Základní operace oro uživatele
-- Custom scrollbar, ikona pro odhlášní uživatele

** Co je nového **
-- Dodělání a zdokonalení operací pro uživatele
    User může prohlížet, přidávat, upravovat a mazat:
       - Témata předmětů, které učí
    User může prohlížet a zpětně stahovat:
       - Testy z předmětů, které učí

** Momentálně rozpracováno **
-- CRUD operace a prohlížení pro admina
    Admin může prohlížet, přidávat, upravovat a mazat:
        - Jakékoliv témata, předměty, uživatele a testy
    Admin může prohlížet, zpětně stahovat a mazat:
        - Jakékoliv testy

-- Testování přípony souborů a jejich velikosti

** Na později **
-- Odkaz na Manuál aplikace
-- Lepší vzhled alertů
-- Rozpoznání, že uživatel není připojen k Wi-Fi
-- Custom logger

-- Tagy v databázi jsou redundantní data => zmiňovaná funkcionalita by mohla být (?), ne však pomocí tags tabulky
-- Abstrakce pro volání dotazů
